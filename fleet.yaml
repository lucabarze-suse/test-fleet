defaultNamespace: default
targetCustomizations:
  - name: all-clusters
    clusterSelector: {}
    helm:
      values:
        # Dynamic value taken from cluster labels
        CLUSTER_DISPLAY_NAME: "${{ get .ClusterLabels 'management.cattle.io/cluster-display-name' }}"
