defaultNamespace: default
helm:
  chart: ./fleet-helm-chart
  releaseName: my-fleet-release
  version: "0.1.0"
  values:
    env:
      CLUSTER_DISPLAY_NAME: "${ .ClusterLabels.management.cattle.io/cluster-display-name }"
