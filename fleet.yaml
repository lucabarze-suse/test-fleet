defaultNamespace: default
targetCustomizations:
  - name: all-clusters
    clusterSelector: {}
    values:
      CLUSTER_ID: ${ get .ClusterLabels global.fleet.clusterLabels.management.cattle.io/cluster-name }
